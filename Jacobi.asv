function x = Jacobi(x, A, b)
while true
    y = x;
    for i = 1 : n
        s = 0;
        for j = 1 : n
            if i != j
                s = s + A[i][j] * y[j];
            end;
        end;
        x[i] = (b[i] - s) / a[i][i];
    end;
end;